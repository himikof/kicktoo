-------------
= Wish List =
-------------

- add LiLo boot support (default will always stay GRUB)
- add swap partition as extended by default instead of primary (this would allow gentoo-lvm.profile and another system to dual boot)
- for gentoo-luks.profile instead of harcoding static-libs deps for cryptsetup use: emerge --autounmask-write && etc-update --automode -5
  but --automode portage feature is not yet in stable
